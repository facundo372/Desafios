
    let costosFijos = Number.parseInt ( prompt ("Ingrese los Costos Fijos: ") )
    let precioDeVenta = Number.parseInt ( prompt ("Ingrese el Precio de Venta: ") )
    let costosVariables = Number.parseInt ( prompt ("Ingrese los costos Variables: ") )

    let resultado =  ( costosFijos / (precioDeVenta - precioDeVenta) )
   
    console.log; ("El punto de equilibrio es: " , resultado) 